---
- name: Setup Server
  hosts: my-server
  remote_user: root

  vars_prompt:
  - name: updateYum
    prompt: "Are you sure you want to update yum?"
    private: yes
    default: n

  roles:
    - common
    - create_user
    - nginx
    - nodejs
    - mariadb
    - ghost
